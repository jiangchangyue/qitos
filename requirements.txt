# Core source install for working from a local clone of QitOS.
#
# This keeps setup.py as the single source of truth for runtime dependencies
# while still giving contributors and evaluators a one-command install path:
#   pip install -r requirements.txt
#
# For optional provider SDKs and contributor tooling, use:
#   pip install -r requirements-dev.txt
#   pip install -e ".[models]"
#   pip install -e ".[benchmarks]"

-e .
